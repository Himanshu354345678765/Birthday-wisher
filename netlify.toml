[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "frontend/build"

[[redirects]]
  from = "/api/*"
  to = "https://YOUR-BACKEND-URL.example.com/:splat"
  status = 200
  force = true


